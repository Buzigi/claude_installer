{
  "_comment": "Claude Code Hooks Configuration - Enforces Agent-First Workflow",
  "_instructions": "This file documents the hooks format. Hooks are defined in ~/.claude/settings.json under the 'hooks' key.",
  "_docs": "https://code.claude.com/docs/en/hooks",
  "_format_note": "Each hook event contains an array of entries. Each entry has a 'matcher' object (use {} to match all tools, or {\"tools\": [\"ToolName\"]} to match specific tools) and a 'hooks' array of command objects.",
  "hooks": {
    "PreToolUse": [
      {
        "_comment": "Delete protection - runs before destructive tool calls",
        "matcher": "Bash|Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude/hooks/pre-tool.sh"
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "_comment": "Operation logger - logs all tool usage",
        "hooks": [
          {
            "type": "command",
            "command": "bash ~/.claude/hooks/post-tool.sh"
          }
        ]
      }
    ],
    "Notification": [
      {
        "_comment": "Agent orchestration notifications",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Agent notification received'"
          }
        ]
      }
    ],
    "Stop": [
      {
        "_comment": "Agent coordination on stop",
        "hooks": [
          {
            "type": "command",
            "command": "echo 'Agent session completed'"
          }
        ]
      }
    ]
  }
}
